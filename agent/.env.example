SERVER_BACKEND_URL=http://127.0.0.1:3001
AGENT_HOST_NAME=
AGENT_RECONNECT_SECONDS=5
# Путь к JSON с паролем (генерируется при каждом старте). Пусто — не писать файл.
AGENT_PASSWORD_JSON_PATH=./agent-credentials.json
